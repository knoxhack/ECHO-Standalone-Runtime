/**
 * Platform-independent save runtime for profiles, manifests, transactional
 * writes, backups, migration plans, corruption checks, and recovery journals.
 */
package dev.echo.standalone.runtime.save;
