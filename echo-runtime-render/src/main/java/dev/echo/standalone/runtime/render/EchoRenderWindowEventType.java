package dev.echo.standalone.runtime.render;

public enum EchoRenderWindowEventType {
    CREATED,
    RESIZED,
    FULLSCREEN_ENTERED,
    WINDOWED_ENTERED,
    CLOSE_REQUESTED,
    CLOSED,
    CRASH_SAFE_CLOSED
}
