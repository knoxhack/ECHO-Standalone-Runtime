/**
 * Application shell boundary for standalone boot, lifecycle, shutdown, safe Ashfall slice orchestration,
 * and deterministic playable mission assembly.
 */
package dev.echo.standalone.runtime.app;
