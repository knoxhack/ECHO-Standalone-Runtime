# ECHO Standalone Release Acceptance Report

Status: current beta acceptance withheld.

Decision date: 2026-06-13.

## Acceptance

Agent 5 does not accept the current standalone runtime for beta tester handoff until the live `runStandaloneBetaReadinessGate` completes again. The checked-in gate report currently records `BLOCKED` preflight evidence, so older green evidence below is historical context rather than current release authority.

When prose and machine-readable evidence disagree, `reports/echo/standalone/beta-readiness-gate.json` and `reports/echo/standalone/beta-readiness-checks.json` win. The beta gate now rejects stale beta-blocking report aggregates by marking them `STALE`; specifically, full-workspace build evidence must be backed by the current `runStandaloneFullWorkspaceBuildSmoke` task state.

## Evidence

- Latest full verification: superseded by current `BLOCKED` beta preflight evidence until rerun.
- Latest beta gate verification: `BLOCKED`, preflight `current-run.incomplete`; rerun `runStandaloneBetaReadinessGate` only when visible packaged/windowed smoke windows are expected.
- Full workspace build smoke: PASS, `runStandaloneFullWorkspaceBuildSmoke` auto-detected `C:/Development/Github/ECHO-Modules`, ran `buildEchoWorkspace -PechoAddonSet=all --no-configuration-cache --no-parallel --rerun-tasks --console=plain`, and is consumed by the beta gate as `workspace.full-addon-build`.
- Beta readiness gate: currently `BLOCKED`; stale full-workspace report JSON cannot satisfy readiness without current task evidence.
- Native Loader execution path: PASS, `reports/echo/standalone/beta-native-loader-execution.json` proves `echo-native-loader-abi-v1` as the standalone boot path, 94 official native entrypoints executed, folder/jar/zip/`.echo-addon` execution covered, unsafe `.echo-addon` archive classpaths rejected, and no Minecraft/NeoForge/Fabric mod-loader command used.
- Asset/resource-pack runtime: PASS, `verifyStandaloneAssetRuntime` is beta-blocking; `asset-index.json`, `asset-mounts.json`, `asset-conflicts.json`, `asset-missing.json`, `resource-pack-plan.json`, and `registry-asset-coverage.json` prove concrete folder/zip/jar resource-pack mounts, jar pack metadata, Minecraft-style texture/model/blockstate/lang resolvers, override conflict detection, missing-asset detection, hot reload, and complete registry asset coverage.
- AdapterCore parity: PASS, 373 parity rows, save contract backed and versioned.
- AdapterCore module coverage: PASS, `reports/echo/standalone/runtime-adaptercore-module-coverage.json` records 132/132 active modules, 0 adapter gaps, 0 unsupported modules, 0 graph issues, and `contractLockedForBeta=true`; `runtime-adaptercore-module-blockers.json` records 0 blockers and `runtime-adaptercore-permission-catalog.json` records 124 known permissions, 118 used permissions, and 0 unknown permissions.
- Playable route: PASS, 17/17 route, 63/63 mission objectives.
- Openlands route/content contract: PASS for beta route proof, but public release remains blocked by `openlands.public-art-content-polish` until `reports/echo/standalone/openlands-full-route-contract.json` records final public art/content review instead of beta-only placeholder asset coverage.
- Save/load: PASS, render checksum `2118313923059fef`, including save-backed dropped item, source/flowing/hardened fluid cells, named-slot autosave/manual backups, corruption restore, OpenGL framebuffer save-slot thumbnails with generated fallback through `reports/echo/standalone/client-save-continue.json`, incompatible-mod Continue blocking, and coordinate-backed editable machine block entity power state with placement metadata, break/reconnect lifecycle, and chunk unload/reload cache persistence.
- ScreenCore shell: PASS, Mods screen shows native runtime content imports by domain and row through `runStandaloneClientModsRuntimeContentSmoke`; settings screens support pointer-driven sliders through `runStandaloneClientSettingsSmoke`; the OpenGL Controls route supports player-facing key rebinding through selectable rows, a pending key-capture state, dirty settings persistence, and reset-to-defaults coverage in `runStandaloneClientKeyBindingsSmoke`; the idle main menu avoids per-tick runtime surface rescans, uses a lightweight passive title heartbeat instead of save-slot/module/resource/workbench rescans, reuses stable ScreenCore title snapshots between input/state changes, keeps the title to player-facing primary actions, caps the idle title panorama at a 19-line lightweight primitive budget, and skips unchanged host settings through `runStandaloneClientScreenRuntimeControllerSmoke` and `runStandaloneClientSettingsControllerSmoke`; Mods and Resource Packs remain available from Options; Options/Diagnostics/Pause and the fatal-error route expose frame-pacing diagnostics, audio backend/device/fallback diagnostics, and an OpenGL client `Export Support Bundle` action through `runStandaloneClientCommandControllerSmoke` and `runStandaloneClientScreenRuntimeControllerSmoke`; machine and terminal ScreenCore surfaces expose restored machine state, power graph, terminal command payloads, machine lifecycle evidence, multi-network machine diagnostics, player-inventory-backed per-instance machine input/output controls, per-instance machine recipe selection, container-backed machine slot persistence, and are covered by catalog, runtime-controller, command-controller, world-interaction, save/continue smokes, and concrete `reports/echo/standalone/client-machine-terminal-surfaces.json` evidence.
- Client session resilience: PASS, `runStandaloneClientSessionResilienceSmoke` and `reports/echo/standalone/client-session-resilience.json` prove the OpenGL native-window focus-loss path pauses active gameplay, unlocks the cursor, clears stale input, preserves the active world, resumes cleanly, survives 8 pause/resume cycles, survives 4 save/quit/continue cycles on the same slot, and ignores stacked gameplay pauses when focus is lost while already paused.
- Block model chunk rendering: PASS, `reports/echo/standalone/client-block-model-chunk-render.json` now records concrete deterministic evidence that mounted Minecraft blockstate/model JSON drives OpenGL chunk-render model selection, face textures, atlas keys, JSON element bounds, vanilla slab/stair/wall/fence/pane/trapdoor/door/cross/crop/bush/leaves/cactus/template-campfire/button/pressure-plate/weighted-pressure-plate/carpet/moss-carpet templates, abstract `block/block` and `thin_block` element-parent models, arbitrary element rotation `rescale` across Y 45-degree, X 22.5-degree, and signed Z -22.5-degree fixtures, `uvlock` texture-orientation parity, and blockstate-rotated arbitrary element cullface/tint behavior through deterministic client smokes.
- Voxel biome chunk rendering: PASS, `reports/echo/standalone/client-voxel-biome-rendering.json` now records concrete deterministic evidence that generated chunk cells expose biome ids, block states carry biome metadata, OpenGL render materials carry biome id/tint color, and client fog/ambience changes are covered by deterministic client smokes.
- Dropped item loop: PASS, live block drops become item entities, mounted datapack block loot can replace self-drops with concrete item drops, nearby pickup transfers them into inventory, and disk restore preserves outstanding drops.
- Fluid mechanics: PASS, source placement, downward flow, horizontal spread, loaded chunk-boundary flow, solid blocking, water/lava hardening, bucket collection/placement, waterlogging host preservation, data-driven scheduled fluid updates, fluid render surface heights, player swim drag/buoyancy, jump-to-swim, crouch sinking, reduced movement speed in fluid, and real client save/load restore are covered by `reports/echo/standalone/world-fluids.json`, `reports/echo/standalone/world-fluid-save-load.json`, `reports/echo/standalone/world-fluid-buckets.json`, `reports/echo/standalone/player-fluid-movement.json`, `reports/echo/standalone/world-fluid-waterlogging.json`, `reports/echo/standalone/world-fluid-scheduled-updates.json`, and `reports/echo/standalone/world-fluid-surface-height.json`.
- Voxel lighting: PASS, `reports/echo/standalone/world-lighting.json` proves 15-level sky/block light, opaque roof shadows, translucent attenuation, emissive block light, loaded cross-chunk propagation, opaque block stopping, and recompute after block changes.
- Weather/hazard gameplay: PASS, `reports/echo/standalone/minecraft-mechanics-contract.json` now requires `weather.gameplay-survival-pressure` from `gameplay-weather.json` and `hazards.gameplay-toxic-ash-damage` from `gameplay-hazards.json`, proving ash-storm heat/visibility pressure and toxic-ash exposure plus health damage.
- Stability soak: PASS, 60-minute equivalent, 720 steps, 12 save/restore cycles.
- Packaged OpenGL EXE evidence: BLOCKED for visible-window probes in this checkout unless `-PechoStandaloneAllowWindowLaunch=true` or `ECHO_STANDALONE_ALLOW_WINDOW_LAUNCH=true` is supplied. The packaged OpenGL client image, portable package SHA-256, strict two-refresh package reproducibility, and launcher icon/installer/version/first-run metadata reports remain concrete, but smoke, strict rehearsal, and 30-/60-minute window monitors now write `BLOCKED_WINDOW_LAUNCH_OPT_IN_REQUIRED` instead of launching and cleaning up a desktop client unexpectedly.
- Player runtime authority: PASS, `reports/echo/standalone/player-runtime-authority.json` proves packaged first launch and launcher handoff target `echo-runtime-client` / `dev.echo.standalone.runtime.client.EchoClientMain`; `EchoRuntimeMain`, headless boot, software renderer, and windowed lifecycle reports are evidence-only and cannot replace the player path.
- Signing helper support: PASS, `reports/echo/standalone/release-signing-failure-rehearsal.json` proves the helper fails closed and writes structured `FAILED` evidence when `-Sign` is requested without a matching certificate; `reports/echo/standalone/release-signing-evidence.json` remains `PENDING_SIGNING` with runtime, launcher, and setup artifacts `NotSigned`. Public signing evidence now also requires expected unsigned SHA-256 matches plus signing operator/run evidence. This report does not satisfy the signed-artifact requirement.
- Manual wall-clock capture support: BLOCKED by the current no-window run. `reports/echo/standalone/manual-wallclock-helper-rehearsal.json` still proves the helper writes structured no-launch evidence and fails closed, but `reports/echo/standalone/manual-wallclock-capture-rehearsal.json` is `BLOCKED` until visible packaged EXE smoke/rehearsal evidence is intentionally refreshed; these reports do not satisfy the human playtest requirement.
- Hardware audio capture support: PENDING, `reports/echo/standalone/audio-hardware-verification.json` must be generated by `scripts/capture-audio-hardware-evidence.ps1` after a tester hears music, UI, SFX, and ambience cues through a real speaker or headset, checks volume/mute behavior, verifies the ScreenCore Diagnostics audio row, exports support-bundle audio diagnostics, and records any crackle/dropout/routing issues. Automated audio output/fallback reports do not satisfy this hardware check.
- Clean install capture support: PASS, `reports/echo/standalone/clean-install-capture-rehearsal.json` proves the clean install/uninstall helper writes structured evidence, records current setup/launcher/helper hashes, and fails closed in no-side-effect rehearsal mode; it does not satisfy the required disposable VM/profile install-uninstall pass.
- External release evidence handoff: BLOCKED_SUPPORT_INPUTS, `reports/echo/standalone/public-release-external-evidence-handoff.json` pins current commands, paths, helper hashes, artifact hashes, and required fields for signing, clean install/uninstall, hardware audio, and human wall-clock evidence runs, but the current no-window run blocked packaged EXE monitor/capture inputs; it does not satisfy those external evidence requirements.
- Full entity AI/spawn mechanics: PASS, `reports/echo/standalone/full-entity-ai-spawn-loop.json` proves source-backed passive, neutral, aquatic, and hostile creature definitions, biome/spawn-rule conditions, live client spawn and spawn-egg flows, AI pursuit/attack, interaction routing, combat loot/XP, dropped-item lifecycle, save/reload, network sync, rendering, AdapterCore entity/spawn/creature/encounter/NPC/combat/loot coverage, and support-bundle inclusion.
- Full survival/player-loop mechanics: PASS, `reports/echo/standalone/full-survival-player-loop.json` proves health, hunger, hydration, damage sources, armor, status effects, difficulty, death/respawn, save/reload, XP, inventory retention/loss, combat, projectile/offhand ammo, tool durability, cursor stack behavior, AdapterCore module coverage, and support-bundle inclusion.
- Full block behavior mechanics: PASS, `reports/echo/standalone/full-block-behavior-matrix.json` proves state properties, collision/shape/outline/cracks, occlusion/transparency, hardness/tool/drop behavior, placement/neighbor dirtying, scheduled/random ticks, block entity persistence/ticking, and support-bundle inclusion.
- Full mechanics parity: PASS, `reports/echo/standalone/full-mechanics-parity-audit.json` records 10/10 public-release mechanics rows passing, including the beta Minecraft-like mechanics contract and every dedicated public-release mechanics coverage report.
- Support bundle: PASS, `reports/echo/standalone/launcher-smoke.json` is concrete `echo.standalone.launcher_smoke.v2` evidence proving the launcher detects the standalone workspace, targets the OpenGL client, exports the tester support-bundle manifest and zip, includes the release signing, clean install/uninstall, and manual wall-clock capture helpers, runs the headless runtime for three ticks, preserves external platform handoff, and produces planning-only repair actions for a broken workspace. `verifyStandaloneLauncherRuntime` also validates that `build/support/EchoStandaloneSupportBundle.zip` contains concrete non-bootstrap launcher reports for runtime launch, detection, verification, repair planning, handoff, support-bundle, and smoke evidence. The OpenGL client now also has a player-facing in-UI support bundle export with frame-pacing diagnostics, audio backend/device/fallback status, save-slot thumbnail source metadata, option tooltip detail, and fatal-error screen snapshot coverage for bug reports; it does not replace the separate public-release evidence runs or real hardware audio verification.

## Beta Warnings

- Human packaged EXE playtest evidence is pending: `manual-playtest-report.json` requires separate 30-minute and 60-minute wall-clock sessions.
- The wall-clock capture workflow is ready and pins the current manual wall-clock capture helper hash, but it does not replace the required human sessions.

## Public Release Decision

Public release is not accepted. `reports/echo/standalone/public-release-gate.json` is now a concrete `BLOCKED` report with blockers for distribution signing/setup, clean install/uninstall evidence, hardware audio verification, Openlands public art/content polish, pending human wall-clock sessions, and visible packaged-window probes that require explicit opt-in. Strict package reproducibility, player runtime authority, launcher icon/installer/version/first-run metadata, clean install capture rehearsal, manual wall-clock helper rehearsal, signing failure rehearsal, full workspace build, and full mechanics parity currently pass; Openlands route evidence passes only as a beta route proof while `openlands-full-route-contract.json` reports `publicReleaseContentPolishReady=false`; visible packaged EXE smoke, strict rehearsal, and 30-/60-minute monitors are `BLOCKED_WINDOW_LAUNCH_OPT_IN_REQUIRED`, and `public-release-external-evidence-handoff.json` is `BLOCKED_SUPPORT_INPUTS` until those package inputs are intentionally refreshed. Bootstrap placeholders for release signing, manual install/uninstall, hardware audio, and manual playtest evidence are explicitly pending and cannot stand in for public-release evidence. `reports/echo/standalone/full-mechanics-parity-audit.json` records 10/10 public-release mechanics rows passing, including controls/commands, data/resource-pack, addon extension, progression/statistics/objectives, worldgen/dimensions/structures, entity AI/spawn, survival/player-loop, presentation, and block behavior. The signing failure rehearsal proves fail-closed behavior, but signed artifacts are still required. Signing/setup now requires running the current `scripts/sign-release-artifacts.ps1` on the release signing machine so runtime, launcher, and setup EXE signatures are timestamped, match a configured release identity, match expected unsigned artifact hashes before signing, and record signing operator/run evidence. Clean install/uninstall evidence now requires running the current `scripts/capture-clean-install-uninstall-evidence.ps1` in a fresh VM or disposable Windows profile/user with VM/profile proof, fresh-snapshot proof, pre-install proof that no ECHO launcher/process state exists, installed launcher hash/signature evidence, support-bundle archive/hash/manifest proof, and post-uninstall cleanup proof. Hardware audio evidence now requires running `scripts/capture-audio-hardware-evidence.ps1` on a real speaker/headset output path and recording cue, volume/mute, Diagnostics, support-bundle, and issue evidence. Signing/setup, clean install/uninstall, hardware audio, Openlands public art/content polish, visible packaged-window probes, and human wall-clock playtest reports must pass before changing public release status.
