# ECHO Standalone Beta Release Readiness

Status: CURRENT LIVE BETA GATE BLOCKED.

Decision date: 2026-06-13.

## Release Gate

The current standalone build is not accepted for beta tester handoff until the live `runStandaloneBetaReadinessGate` completes again. The checked-in gate report currently records `BLOCKED` preflight evidence, not a completed green aggregation, after the broad gate run was interrupted.

This document must not be used as release authority when `reports/echo/standalone/beta-readiness-gate.json` disagrees with it. The gate now treats beta-blocking report aggregates as current-run evidence: stale report JSON is marked `STALE` and blocks readiness instead of silently preserving an older green result.

Evidence:

- `reports/echo/standalone/beta-readiness-gate.json`: `BLOCKED`, preflight `current-run.incomplete`, and not proof of beta readiness.
- Historical green gate notes below are superseded until a new uninterrupted `runStandaloneBetaReadinessGate` run rewrites the gate reports to a completed ready state.
- `runStandaloneBetaReadinessGate` now depends directly on `runStandaloneFullWorkspaceBuildSmoke` and records `fullWorkspaceBuildCurrentRunAccepted`; stale full-workspace report JSON cannot satisfy `workspace.full-addon-build`.
- `reports/echo/standalone/beta-native-loader-execution.json`: `PASS`, proving the standalone boot path is `echo-native-loader-abi-v1`, 94 official native entrypoints executed, folder/jar/zip/`.echo-addon` execution is covered, unsafe `.echo-addon` archive classpaths are rejected, and no Minecraft/NeoForge/Fabric mod-loader command was used.
- `reports/echo/standalone/beta-readiness-checks.json`: `PASS` for `workspace.full-addon-build`, `assets.resource-pack-mounts`, `mechanics.weather-hazard-gameplay`, `mechanics.voxel-lighting`, and `mechanics.block-model-uvlock`, proving all-addon workspace build evidence, folder/zip/jar resource-pack mounts, standalone gameplay weather/hazard pressure, voxel lighting, and UV-lock block model behavior are beta-blocking.
- `reports/echo/standalone/asset-index.json`, `asset-mounts.json`, `asset-conflicts.json`, `asset-missing.json`, and `resource-pack-plan.json`: concrete `v2` asset runtime evidence proving directory, zip, and jar mounts, jar pack metadata, Minecraft-style texture/model/blockstate/lang resolvers, override conflict detection, missing-asset detection, and hot-reload addition of `ashfall:icons/hazard.png`.
- `reports/echo/standalone/client-block-model-chunk-render.json`: `PASS`, concrete `echo.standalone.client_block_model_chunk_render.v1` evidence proving mounted blockstate/model JSON reaches chunk upload, including vanilla crop/bush/leaves/cactus, template-campfire, button/pressed-button, pressure-plate/weighted-pressure-plate, carpet/moss-carpet templates, abstract `block/block` and `thin_block` element-parent models, arbitrary element rotation `rescale` for Y 45-degree, X 22.5-degree, and signed Z -22.5-degree elements, a non-rescaled control, and blockstate-rotated arbitrary element cullface/tint behavior.
- `reports/echo/standalone/packaged-exe-wallclock-smoke.json`: `BLOCKED_WINDOW_LAUNCH_OPT_IN_REQUIRED`, because visible packaged-window probes now require explicit `-PechoStandaloneAllowWindowLaunch=true` or `ECHO_STANDALONE_ALLOW_WINDOW_LAUNCH=true`.
- `reports/echo/standalone/packaged-exe-wallclock-strict-rehearsal.json`: `BLOCKED_WINDOW_LAUNCH_OPT_IN_REQUIRED`, because visible packaged-window strict rehearsal now requires explicit window-launch opt-in.
- `reports/echo/standalone/manual-wallclock-helper-rehearsal.json`: `PASS`, proving the manual wall-clock helper writes structured no-launch evidence, records current packaged EXE/portable ZIP/helper hashes, exits through the expected incomplete-evidence path, and does not replace either required human playtest session.
- `reports/echo/standalone/manual-wallclock-capture-rehearsal.json`: `BLOCKED`, because visible packaged EXE smoke/rehearsal evidence was intentionally skipped unless window launch is explicitly opted in; it still pins the helper hash and does not replace the required human sessions.
- `reports/echo/standalone/packaged-exe-wallclock-strict-30m.json`: `BLOCKED_WINDOW_LAUNCH_OPT_IN_REQUIRED`, so the previous long monitor evidence is not accepted as current release evidence until the visible-window run is intentionally repeated.
- `reports/echo/standalone/packaged-exe-wallclock-strict-60m.json`: `BLOCKED_WINDOW_LAUNCH_OPT_IN_REQUIRED`, so the previous long monitor evidence is not accepted as current release evidence until the visible-window run is intentionally repeated.
- `reports/echo/standalone/full-audio-particles-weather-ambience.json`: `PASS`, full public-release presentation evidence for audio buses/clips/cues, device fallback, volume controls, diagnostics, subtitles, accessibility settings, particles, weather, ambience, resource hot reload, missing-asset fallback, and support-bundle contract inclusion.
- `reports/echo/standalone/audio-hardware-verification.json`: `PENDING_HARDWARE_AUDIO_RUN`, explicit real speaker/headset verification evidence still required for public release; deterministic Java Sound, fallback, ScreenCore Diagnostics, and support-bundle audio reports do not replace the physical output check.
- `reports/echo/standalone/full-command-keybind-debug-chat.json`: `PASS`, full public-release controls evidence for command dispatch, permission policy, keybind registration/routing, rebinding persistence, terminal/chat routing, debug overlay diagnostics, and support-bundle contract inclusion. `runStandaloneClientKeyBindingsSmoke` also proves the OpenGL client Controls route renders selectable keybinding rows, enters a player-facing pending rebind state, accepts a supported key, marks client settings dirty for persistence, and still supports reset to defaults.
- `reports/echo/standalone/full-data-pack-runtime-coverage.json`: `PASS`, full public-release data/resource-pack evidence for data registries, schemas, recipes, loot tables, tags, world/entity/hazard data domains, mounted folder/zip/jar resources, registry-backed block/item assets, conflict priority, missing-asset recovery, hot reload, AdapterCore module coverage, and support-bundle contract inclusion.
- `reports/echo/standalone/full-addon-extension-surface.json`: `PASS`, full public-release addon extension evidence for descriptor-only containment, executable Native Loader ABI safety, folder/jar/zip/`.echo-addon` archive execution, bad-addon fail-closed behavior, AdapterCore extension domains, real module lifecycle/services, save compatibility recovery, and support-bundle contract inclusion.
- `reports/echo/standalone/full-progression-statistics-objectives.json`: `PASS`, full public-release progression evidence for objective/mission lifecycle, playable-route advancement equivalents, tracked XP/level/milestone/notification/action counters, save/load persistence, declarative scripting objective actions, tutorial/progression module coverage, incompatible-addon recovery, retry checkpoint recovery, and support-bundle contract inclusion.
- `reports/echo/standalone/full-worldgen-dimensions-structures.json`: `PASS`, full public-release worldgen evidence for source-backed biomes, cave/ore resources, structures/landmarks, POIs, dimensions, region/chunk persistence, chunk streaming, waystone transition save/reload, lighting, weather, fluid interaction, AdapterCore worldgen module coverage, and support-bundle contract inclusion.
- `reports/echo/standalone/full-entity-ai-spawn-loop.json`: `PASS`, full public-release entity evidence for source-backed passive, neutral, aquatic, and hostile creature definitions, biome/spawn-rule conditions, live client spawn and spawn-egg flows, AI pursuit/attack, interaction routing, combat loot/XP, dropped-item lifecycle, save/reload, network sync, rendering, AdapterCore entity/spawn/creature/encounter/NPC/combat/loot coverage, and support-bundle contract inclusion.
- `reports/echo/standalone/full-survival-player-loop.json`: `PASS`, full public-release survival/player-loop evidence for health, hunger, hydration, damage sources, armor, status effects, difficulty, death/respawn, save/reload, XP, inventory retention/loss, combat, projectile/offhand ammo, tool durability, cursor stack behavior, AdapterCore module coverage, and support-bundle contract inclusion.
- `reports/echo/standalone/full-block-behavior-matrix.json`: `PASS`, full public-release block behavior matrix evidence for state properties, collision/shape/outline/cracks, occlusion/transparency, hardness/tool/drop behavior, placement/neighbor dirtying, scheduled/random ticks, block entity persistence/ticking, and support-bundle contract inclusion.
- `reports/echo/standalone/full-mechanics-parity-audit.json`: `PASS`, 10/10 public-release mechanics rows passing across beta Minecraft-like mechanics, controls/commands, data/resource-pack, addon extension, progression/statistics/objectives, worldgen/dimensions/structures, entity AI/spawn, survival/player-loop, presentation, and full block behavior coverage.
- `reports/echo/standalone/openlands-full-route-contract.json`: `PASS` for the beta first-hour route contract, but `publicReleaseContentPolishReady=false` and `publicReleasePolishStatus=BLOCKED_PUBLIC_ART_REVIEW` because the Openlands asset manifest still records beta-only placeholder coverage pending the public alpha art/content review.
- `reports/echo/standalone/launcher-smoke.json`: concrete `echo.standalone.launcher_smoke.v2` evidence proving standalone workspace detection, OpenGL handoff target, support-bundle manifest/archive export, headless three-tick launch, external platform handoff preservation, and broken-workspace repair planning. `verifyStandaloneLauncherRuntime` now opens `build/support/EchoStandaloneSupportBundle.zip` and rejects archived launcher evidence if any report is still a bootstrap placeholder.
- `reports/echo/standalone/world-fluids.json`: `PASS`, voxel fluid source placement, downward flow, horizontal spread, loaded chunk-boundary flow, solid blocking, and water/lava hardening.
- `reports/echo/standalone/world-fluid-save-load.json`: `PASS`, the real client chunk TSV codec restores source, flowing, and hardened standalone fluid cells with fluid metadata.
- `reports/echo/standalone/world-fluid-buckets.json`: `PASS`, real gameplay right-click collects source water into `minecraft:water_bucket`, and water/lava buckets place standalone source cells through the client session item path.
- `reports/echo/standalone/player-fluid-movement.json`: `PASS`, the player controller detects standalone fluid cells and applies swim drag, buoyancy, jump-to-swim, crouch sinking, and reduced horizontal movement speed.
- `reports/echo/standalone/world-fluid-waterlogging.json`: `PASS`, source placement, flow, bucket placement/collection, and real client save/load preserve waterloggable host block identity with fluid metadata.
- `reports/echo/standalone/world-fluid-scheduled-updates.json`: `PASS`, data-driven `fluidTickInterval` metadata gates runtime scheduled fluid propagation and live client gameplay scheduled ticks with dirty chunk evidence.
- `reports/echo/standalone/world-fluid-surface-height.json`: `PASS`, `fluidLevel` metadata drives bounded CPU mesh faces, OpenGL upload vertices, and software renderer surface heights while falling fluids remain full-height columns.
- `reports/echo/standalone/world-lighting.json`: `PASS`, voxel sky light, opaque roof shadows, translucent attenuation, emissive block light, loaded cross-chunk block light propagation, opaque block stopping, and dynamic recompute after block changes.
- `reports/echo/standalone/gameplay-weather.json` and `reports/echo/standalone/gameplay-hazards.json`: `PASS`, ash-storm weather applies heat-stress and visibility pressure, and toxic ash applies exposure plus health damage to the player.
- `reports/echo/standalone/client-voxel-biome-rendering.json`: `PASS`, concrete `echo.standalone.client_voxel_biome_rendering.v1` evidence proving generated chunk biome ids, block-state biome metadata, render material biome ids/tints, fog/ambience switching, debug overlay text, and atlas-source cache behavior.
- `reports/echo/standalone/client-machine-terminal-surfaces.json`: `PASS`, concrete `echo.standalone.client_machine_terminal_surfaces.v1` evidence proving the title menu stays on player-facing primary actions, reuses stable ScreenCore snapshots while idle, avoids full runtime surface rescans through lightweight passive heartbeats, caps the idle title panorama at a 19-line lightweight primitive budget, and that ScreenCore machine and terminal surfaces expose AdapterCore counts, restored machine state, power graph rows, inventory-backed input/output controls, per-instance recipe selection, registered terminal routes, terminal command payloads, live diagnostics, frame-pacing counters, audio device/fallback diagnostics, fatal-error routing, and the OpenGL client support-bundle export action.
- `reports/echo/standalone/client-session-resilience.json`: `PASS`, concrete `echo.standalone.client_session_resilience.v1` evidence proving the OpenGL client focus-loss path pauses active gameplay, unlocks the cursor, clears stale gameplay input, preserves the active world, resumes cleanly, tolerates 8 pause/resume cycles, survives 4 save/quit/continue cycles on the same slot, and does not stack another gameplay pause when focus is lost while already paused.
- `reports/echo/standalone/client-save-continue.json`: `PASS`, concrete `echo.standalone.client_save_continue.v1` evidence proving OpenGL framebuffer save-slot thumbnail capture, `client/thumbnail.png` manifest metadata, World Select texture eligibility, corrupt-thumbnail deterministic fallback, disk Continue restore, incompatible runtime content blocking, backup/migration readiness, and delete-world Continue disabling.
- `reports/echo/standalone/packaged-opengl-client-image.json`: `PASS`, jpackage app image config targets `dev.echo.standalone.runtime.client.EchoClientMain`, excludes the superseded `EchoRuntimeMain`, and records the current portable zip SHA-256.
- `reports/echo/standalone/player-runtime-authority.json`: `PASS`, proving the player-facing standalone launch authority is `echo-runtime-client` / `dev.echo.standalone.runtime.client.EchoClientMain`, while `echo-runtime-app`, headless boot, software renderer, and windowed lifecycle paths remain evidence-only.
- `reports/echo/standalone/portable-zip-install-smoke.json`: `BLOCKED_WINDOW_LAUNCH_OPT_IN_REQUIRED`, because the portable ZIP install smoke would launch a visible extracted EXE and now requires explicit window-launch opt-in.
- `reports/echo/standalone/beta-distribution-release-contract.json`: `BLOCKED`, because the package image, metadata, and reproducibility checks can pass in a no-window run but portable ZIP launch/removal, packaged EXE smoke, strict visible-window rehearsal, and manual wall-clock capture support are blocked until visible-window probes are intentionally opted in.
- `reports/echo/standalone/distribution-icons.json`, `distribution-installer.json`, `distribution-first-run-checks.json`, and `distribution-version-metadata.json`: concrete `PASS` reports replacing bootstrap placeholders for launcher icon hashes/config, NSIS setup artifact/latest.yml metadata, packaged first-run config, and version metadata.
- `reports/echo/standalone/distribution-package-reproducibility-strict.json`: `PASS`, strict two-refresh OpenGL portable package reproducibility with matching bytes, entry count, and SHA-256.
- `reports/echo/standalone/distribution-signing-setup.json`: `PENDING_SIGNING`, public release now requires runtime, launcher, and setup EXE signatures to be timestamped, to match a configured release identity from `build.win.certificateSubject`, `build.win.certificateSha1`, or `build.win.publisherName`, and to be backed by current helper-generated `PASS` signing evidence with expected unsigned SHA-256 matches plus signing operator/run evidence; the current checkout still has signing disabled and no release identity configured, the report now pins `scripts/sign-release-artifacts.ps1` as the release signing helper, and `release-signing-evidence.json` is regenerated as real `PENDING_SIGNING` evidence instead of bootstrap `PASS` evidence.
- `reports/echo/standalone/release-signing-failure-rehearsal.json`: `PASS`, proving the signing helper writes structured `FAILED` evidence and refuses to sign when `-Sign` is requested without a matching code-signing certificate; it does not replace signed runtime, launcher, or setup artifacts.
- `reports/echo/standalone/distribution-install-uninstall-evidence.json`: `PENDING_MANUAL_OR_VM_RUN`, bootstrap manual install/uninstall evidence is rejected as `PLACEHOLDER`, raw bootstrap manual install/uninstall evidence is normalized to `PENDING_MANUAL_OR_VM_RUN`, and a disposable-profile/VM evidence template was regenerated with required VM/profile proof, fresh-snapshot proof, pre-install clean-state proof, installed launcher hash/signature capture, support-bundle archive/hash/manifest evidence, post-uninstall cleanup proof, and the exact `scripts/capture-clean-install-uninstall-evidence.ps1` hash expected for capture.
- `reports/echo/standalone/clean-install-capture-rehearsal.json`: `PASS`, proving the clean install/uninstall capture helper writes structured evidence in no-side-effect rehearsal mode, records current setup/launcher/helper hashes, exits through the expected incomplete-evidence path, and does not replace the required disposable VM/profile install-uninstall run.
- `reports/echo/standalone/public-release-gate.json`: `BLOCKED`, with current blockers for distribution signing/setup, clean install/uninstall evidence, real hardware audio verification, Openlands public art/content polish, visible packaged-window probes that require explicit opt-in, and pending human wall-clock sessions.
- `reports/echo/standalone/public-release-external-evidence-handoff.json`: `BLOCKED_SUPPORT_INPUTS`, because the current no-window run blocked packaged EXE monitor/capture inputs; it still includes machine-readable command and hash handoff for signing, clean install/uninstall, hardware audio, and human wall-clock sessions once the package evidence is intentionally refreshed.
- `reports/echo/standalone/manual-playtest-report.json`: `PENDING`, explicit non-automated evidence template with 30-minute and 60-minute packaged OpenGL playtest sessions still required.

## Required Integration Checks

- Beta readiness: BLOCKED until `runStandaloneBetaReadinessGate` completes and rewrites current-run gate reports.
- AdapterCore parity: PASS, 373 parity rows, save contract `echoashfallprotocol:save/live_mission_state`.
- Native Loader execution path: PASS, `beta-native-loader-execution.json` aggregates Native Loader ABI, official real-module execution, folder/jar/zip/`.echo-addon` archive execution, and unsafe `.echo-addon` archive classpath rejection as a beta-blocking proof.
- Asset/resource-pack runtime: PASS, `verifyStandaloneAssetRuntime` is beta-blocking and proves concrete `asset-index`, `asset-mounts`, `asset-conflicts`, `asset-missing`, `resource-pack-plan`, and registry asset coverage reports, including folder, zip, and jar resource-pack mounts with override/missing/hot-reload behavior.
- AdapterCore module coverage: PASS, `reports/echo/standalone/runtime-adaptercore-module-coverage.json` records 132/132 active modules, 0 adapter gaps, 0 unsupported modules, 0 graph issues, and `contractLockedForBeta=true`; `runtime-adaptercore-module-blockers.json` records 0 blockers and `runtime-adaptercore-permission-catalog.json` records 124 known permissions, 118 used permissions, and 0 unknown permissions.
- Full playable route: PASS, 63/63 mission objectives, route 17/17.
- Save/load: PASS, world edits/player/hotbar/dropped items/source-flowing-hardened fluid cells/coordinate-backed editable machine block entity power state with placement metadata, break/reconnect lifecycle, chunk unload/reload cache persistence/mission/render checksum restored; named save slots cover autosave/manual backups, corruption restore, OpenGL framebuffer thumbnail capture with generated fallback through `reports/echo/standalone/client-save-continue.json`, manual-only migration prompt, and incompatible-mod Continue blocking.
- ScreenCore shell polish: PASS, lightweight title panorama with a 19-line idle primitive budget, stage-keyed loading tips, pointer-driven settings sliders, accessibility settings, language settings, player-facing Controls key rebinding, lightweight title-menu passive heartbeats that avoid save-slot/module/resource/workbench rescans, stable idle title snapshot cache hits, player-first title actions with Mods and Resource Packs retained under Options, unchanged-settings apply skipping, frame-pacing and audio device/fallback Diagnostics rows, debug-overlay counters, fatal-error screen routing, OpenGL client UI support-bundle export, Mods runtime content inventory, and coordinate-backed editable machine/terminal ScreenCore surfaces with state payload rows, machine lifecycle evidence, multi-network machine diagnostics, player-inventory-backed per-instance machine input/output controls, per-instance recipe selection, and container-backed machine slot persistence are covered by deterministic client smokes and concrete `reports/echo/standalone/client-machine-terminal-surfaces.json` evidence.
- Client session resilience: PASS, `runStandaloneClientSessionResilienceSmoke` and `reports/echo/standalone/client-session-resilience.json` prove the native-window focus-loss path releases cursor/input and routes active gameplay to Pause before gameplay input runs, Resume returns to the same world, repeated pause/resume remains stable, and repeated save/quit/continue restores the same slot.
- Block model chunk rendering: PASS, `reports/echo/standalone/client-block-model-chunk-render.json` now records concrete harness evidence that mounted Minecraft blockstate/model JSON reaches the OpenGL chunk-render planning path for blockstate variant selection, parent-template face textures, atlas keys, JSON element bounds, vanilla slab/stair/wall/fence/pane/trapdoor/door/cross/crop/bush/leaves/cactus/template-campfire/button/pressure-plate/weighted-pressure-plate/carpet/moss-carpet templates, abstract `block/block` and `thin_block` element-parent models, arbitrary element rotation `rescale` across Y 45-degree, X 22.5-degree, and signed Z -22.5-degree fixtures, `uvlock` texture-orientation parity, and blockstate-rotated arbitrary element cullface/tint behavior.
- Voxel biome chunk rendering: PASS, `reports/echo/standalone/client-voxel-biome-rendering.json` now records concrete harness evidence that generated chunks carry biome ids through block state and chunk cell metadata, while OpenGL chunk materials carry biome id/tint color and client fog/ambience state switches by biome.
- Visible HUD feedback: PASS, held item preview, block-break feedback, and action particles are covered by deterministic framebuffer pixel evidence.
- Save recovery: PASS through save profile flow warning, backup restore path, and incompatible-mod blocked-slot recovery prompt.
- Inventory UX: PASS, drag, split, hotbar assignment, tooltip, data-tag-aware workbench recipes, data-driven block loot drops, disabled states, keyboard/mouse flow.
- Dropped item loop: PASS, `runStandaloneClientDroppedItemSmoke` covers live dropped block items, nearby pickup, debug overlay counts, and save/restore through `client/dropped_items.tsv`; `runStandaloneClientDroppedItemPhysicsSmoke` covers fixed-step dropped-item physics budget and settled-item chunk-index skipping; `runStandaloneClientBlockLootDataSmoke` covers mounted datapack block loot and item-tag resolution into live dropped items.
- Terminal branching: PASS, terminal framebuffer states, diagnostics, extraction authorization, registered terminal route bridging, terminal command payloads, and terminal surface back-stack behavior.
- Support bundle diagnostics: PASS, `launcher-smoke.json` and the exported release support-bundle zip carry concrete non-placeholder launcher evidence for runtime launch, detection, verification, repair planning, handoff, support-bundle manifest/archive export, the release signing helper, the clean install/uninstall and manual wall-clock capture helpers, full-mechanics parity audit, full block-behavior mechanics evidence, full command/keybind/debug/chat mechanics evidence, full data/resource-pack mechanics evidence, full addon extension mechanics evidence, full progression/statistics/objectives mechanics evidence, full worldgen/dimensions/structures mechanics evidence, full entity AI/spawn mechanics evidence, full survival/player-loop mechanics evidence, full presentation mechanics evidence, 30-/60-minute strict monitor reports, generated module diagnostics, runtime registry fingerprints, bounded save metadata, incompatible-mod save recovery evidence, ScreenCore route, machine/terminal state-payload evidence, blockstate/model chunk-render evidence, voxel biome id/tint rendering evidence, machine placement/break lifecycle evidence, chunk cache persistence evidence, ScreenCore multi-network machine diagnostics, per-instance machine input/output evidence, machine recipe/container slot evidence, fatal-error ScreenCore route evidence, distribution install/uninstall evidence status, AdapterCore target diff artifacts, and descriptor-only module lifecycle traces. The OpenGL client also exposes an in-UI `Export Support Bundle` action from Options/Diagnostics/Pause and the fatal-error screen that writes a local client diagnostics zip with frame-pacing diagnostics, structured audio backend/device/fallback diagnostics, and the current ScreenCore snapshot; it is for bug reports and does not replace public-release signing, VM install, hardware audio verification, or human playtest evidence.
- Stability soak: PASS, 60-minute equivalent, 720 steps, 60 frames, 12 save/restore cycles.
- Fluid mechanics: PASS, `runStandaloneVoxelFluidSmoke`, `runStandaloneClientFluidSaveSmoke`, `runStandaloneClientFluidBucketSmoke`, `runStandalonePlayerFluidMovementSmoke`, `runStandaloneClientFluidWaterloggingSmoke`, `runStandaloneClientFluidScheduledUpdateSmoke`, and `runStandaloneClientFluidSurfaceHeightSmoke` are beta-blocking and prove source blocks, flow levels, descending fluid, cross-chunk propagation, solid blocking, water/lava hardening, bucket collection/placement, waterlogging host preservation, data-driven scheduled fluid updates, fluid render surface heights, player swim drag/buoyancy, jump-to-swim, crouch sinking, reduced movement speed in fluid, and real client save/load restore without a native mod-loader handoff.
- Voxel lighting: PASS, `runStandaloneVoxelLightingSmoke` and `reports/echo/standalone/world-lighting.json` prove 15-level sky/block light, roof occlusion, translucent attenuation, emissive source seeding, loaded chunk-boundary propagation, opaque blockers, and recompute after a block change.
- Weather and hazard gameplay: PASS, `verifyStandaloneGameplayRuntime` is beta-blocking and `reports/echo/standalone/minecraft-mechanics-contract.json` now requires `weather.gameplay-survival-pressure` and `hazards.gameplay-toxic-ash-damage` alongside the world weather/hazard queries.
- Full workspace build smoke: PASS, `runStandaloneFullWorkspaceBuildSmoke` auto-detected `C:/Development/Github/ECHO-Modules`, ran `buildEchoWorkspace -PechoAddonSet=all --no-configuration-cache --no-parallel --rerun-tasks --console=plain`, completed successfully, and is consumed by the beta gate as `workspace.full-addon-build`.
- Packaged runtime refresh: PASS, `refreshStandalonePackagedRuntimeImage` rebuilds the `echo-runtime-client` jpackage app image and portable zip before packaged EXE smoke; it now emits concrete icon, installer, first-run config, and version metadata reports. Visible packaged EXE probes (`runStandalonePortableZipInstallSmoke`, `runStandalonePackagedOpenGlImageSmoke`, and strict monitor tasks) require `-PechoStandaloneAllowWindowLaunch=true` or `ECHO_STANDALONE_ALLOW_WINDOW_LAUNCH=true`; without that opt-in `writeStandaloneVisibleWindowLaunchOptInReports` writes `BLOCKED_WINDOW_LAUNCH_OPT_IN_REQUIRED` evidence for all visible-window probe reports so aggregate gates do not launch, clean up, or preserve stale green desktop-window evidence.

## Accepted Beta Warning

- Human packaged EXE playtest evidence remains pending: `manual-playtest-report.json` requires separate 30-minute and 60-minute wall-clock sessions. This is accepted for beta tester handoff and blocks public release only.
- The capture workflow is concrete but currently blocked by the no-window safety policy: `manual-wallclock-helper-rehearsal.json` proves the helper fails closed in no-launch mode, while `manual-wallclock-capture-rehearsal.json` is `BLOCKED` until visible packaged EXE probes are intentionally rerun with opt-in; none of that replaces the required human sessions.
- Openlands route evidence is accepted only as a beta route proof while `openlands-full-route-contract.json` reports `publicReleaseContentPolishReady=false`; final public art/content review evidence must pass before public release.

## Public Release Gaps

- Distribution signing/setup, clean install/uninstall evidence, real hardware audio verification, Openlands public art/content review, and human wall-clock playtests are not accepted as completed public-release evidence in this checkout unless their reports are regenerated by their dedicated tasks/manual capture process and pass. Bootstrap placeholders for release signing, manual install/uninstall, hardware audio, and manual playtest evidence are explicitly pending and cannot stand in for public-release evidence. Openlands route evidence can pass for beta, but the public gate now blocks while `openlands-full-route-contract.json` reports beta-only placeholder coverage instead of final public art/content polish. `public-release-external-evidence-handoff.json` pins the current commands, paths, helper hashes, and artifact hashes for the external runs, but it is only a handoff manifest and is currently `BLOCKED_SUPPORT_INPUTS` until visible packaged EXE evidence is intentionally refreshed. Signing/setup requires running the current `scripts/sign-release-artifacts.ps1` on the release signing machine so runtime, launcher, and setup EXE signatures are timestamped, match a configured release identity, match expected unsigned artifact hashes before signing, and record signing operator/run evidence. Clean install/uninstall evidence requires running the current `scripts/capture-clean-install-uninstall-evidence.ps1` in a fresh VM or disposable Windows profile/user, VM/profile proof, fresh-snapshot proof, pre-install proof that no ECHO launcher/process state exists, installed launcher hash/signature evidence, support-bundle archive/hash/manifest proof, and post-uninstall cleanup proof. Hardware audio evidence requires running `scripts/capture-audio-hardware-evidence.ps1` after checking music, UI, SFX, ambience, volume/mute behavior, Diagnostics, and support-bundle audio rows through a real speaker or headset. Full mechanics parity now has current `PASS` evidence through dedicated public-release reports, including full block behavior matrix coverage. The signing failure rehearsal and the clean install and manual wall-clock helper rehearsals prove their helpers can fail closed, but they do not remove the public-release blockers. Strict package reproducibility, launcher icon/installer/version/first-run metadata, and full workspace build smoke have current `PASS` evidence; visible packaged EXE smoke, strict rehearsal, and 30-/60-minute monitors are `BLOCKED_WINDOW_LAUNCH_OPT_IN_REQUIRED` until rerun with explicit window-launch opt-in.

## Signoff

Agent 5 release decision: beta acceptance is withheld until the live beta gate completes green again. It is not public-release-ready until the manual and distribution evidence above is completed.
